# .github

`apm-handson-org` のプロファイルリポジトリ。

`apm-policy.yml` は APM (Agent Package Manager) の組織ポリシーで、
`apm audit --ci --policy org` から自動ディスカバリされます。

Zenn 記事「Agent Package Manager (APM) をハンズオンで触ってみる」の一部です。
